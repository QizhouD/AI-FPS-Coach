# AI 练习复盘模式设计方案

本文档定义「训练后复盘」这一产品形态的目标、边界、现状盘点与实施顺序。

与 `VISION_NEXT_STEPS_ZH.md` 的关系：那份文档描述的是视觉模块的**依赖安装、模型准备与接口验证**
步骤，仍然适用。本文档定义其上层的**产品形态与实施顺序**，在产品决策上优先。

---

## 1. 产品形态

玩家在靶场/训练图完成一轮练习，练习过程由 OBS 录屏。练习结束后把录像交给客户端，
后端用 YOLO 逐帧标出敌人框与头部位置，客户端在战术大屏上播放录像并叠加检测结果，
配合准心位置给出瞄准分析。

**核心循环**：打一轮（30~90 秒）→ 分析 → 看偏差与习惯 → 调整 → 再打一轮。

**关键特征**：游戏进行中不运行任何分析程序，只有 OBS 在录制。分析发生在练习之后。

**明确不做**：
- 不做实时叠加，不要求双屏。
- 不读取游戏内存，不注入进程，只处理录像画面。
- 不做鼠标接管、不做任何自动瞄准或辅助瞄准输出。

---

## 2. 形态决策记录

本项目考虑过三种形态，此处记录演进与理由。

**A. 实时陪练（已否决）**：客户端实时显示游戏画面并叠加检测框。否决原因有三：

1. **交互不成立**。实时叠加要求玩家一边打一边看另一个窗口。单屏物理上做不到，
   双屏也需分心。这不是体验折扣，是交互本身无法完成。
2. **反馈来不及**。对枪的决策窗口是 100~300 ms，而端到端延迟（OBS 缓冲 + 捕获 +
   推理 + 绘制）在 CPU 上预计 200~400 ms。提示出现时这一枪已经结束。
3. **精度被迫让步**。实时要求小模型、低分辨率、低抽帧率，检出质量显著下降。

**B. 比赛复盘（保留，非当前重点）**：解析 `.dem` 得到精确数据。已验证 `demoparser2` 能提供
逐 tick 的 `pitch`、`yaw`（真实视角角度）、全员 `X/Y/Z`、`weapon_fire`、`hitgroup`（命中部位）。
实测样本（sh1ro，60 发）：步枪/手枪偏差 0.4°~2.4°，投掷物 14°~48°。
精度远高于视觉推断，但只能用于正式比赛后的分析，且不体现视觉模型能力。
本方案将其作为**视觉模型的真值来源**保留，见第 9 节 P2。

**C. 训练后复盘（当前方案）**：录屏 + 离线视觉分析。同时解决 A 的三个问题：
无需双屏、无延迟约束、可用最大模型与原生分辨率。并额外获得音频轨、结果可复现两项能力。

---

## 3. 工作流

```
CS2 靶场练习（OBS 录制中）
        ↓  练习结束，停止录制
录像文件（.mp4，含音频）
        ↓  客户端 IMPORT VIDEO
POST /api/v1/vision/video  → 后台 job 逐帧推理
        ↓  轮询 /api/v1/vision/jobs/<id>
战术大屏播放录像 + 叠加敌人框/头部/推荐瞄准点/偏移连线
        ↓
偏差统计与习惯分析
```

录制建议：60 FPS、1080p、含音频轨（音频用于开火时刻检测，见第 7 节）。
分析按**单轮**提交（30~90 秒），不要整场几十分钟一次提交，原因见 5.2。

---

## 4. 现状盘点：已经具备的部分

这条链路后端与客户端**都已实现**，这是选择该形态的最大技术优势。

| 环节 | 承担者 | 状态 |
| --- | --- | --- |
| 视频导入 | 战情室 `IMPORT VIDEO` 按钮 + `NativeVideoFilePicker` | 可用 |
| 视频播放 | `TacticalScreenController` + `VideoPlayer` → 战术大屏 | 可用 |
| 提交分析 | `POST /api/v1/vision/video`，`VisionJobManager.submit()` | 可用 |
| 逐帧推理 | 后台线程 `cv2.VideoCapture` 按 `sample_rate` 抽帧 → `analyze_image()` | 代码完整，缺依赖与权重 |
| 进度轮询 | `GET /api/v1/vision/jobs/<id>`，客户端每 0.5 s 轮询 | 可用 |
| 标签解析 | `normalize_label()`：`ct*`→CT、`t*`→T、含 `head`→头部 | 可用 |
| 瞄准选点 | `select_recommended_aim()` 取离准心最近的头部框 | 可用 |
| 结果回放 | `ApplyVideoJobResult()` 按 `timestamp <= videoPlayer.time` 取当前帧结果 | 可用，支持拖拽/暂停/跳转 |
| 叠加绘制 | `VisionInferenceOverlay` 画敌人框、推荐点、偏移连线、指标文字 | 可用 |

准心不需要模型：第一人称视角下准心恒在屏幕中心，`FPS_VISION_CROSSHAIR_BASELINE=true`
即用 `(0.5, 0.5)` 作为基线，足够使用。这使 `VISION_NEXT_STEPS_ZH.md` 第 3~5 节的
准心标注与训练工作**在本形态下不是必需项**。

---

## 5. 缺口清单

### 5.1 Python 视觉依赖未安装（阻塞）

`torch`、`ultralytics`、`opencv-python`、`onnxruntime` 在开发机 `.venv` 中可能不存在。
运行机执行 `.\Backend\setup-vision.ps1`（先 CUDA torch，再 `requirements.txt`）。
若先跑 `run.ps1`，会装上 CPU 版 torch，`/api/v1/vision/frame` 即使不 503 也用不上显卡。

### 5.2 没有模型权重（阻塞）

任何路径下都没有 `.pt` / `.onnx`。选型见第 6 节。

### 5.3 媒体根目录限制

`VisionJobManager.validate_path()` 要求视频必须位于 `FPS_VISION_MEDIA_ROOT` 之内，
否则返回 400。OBS 默认录制到用户视频目录，因此需要把该环境变量指向 OBS 输出目录，
或在客户端导入时复制到媒体目录。

### 5.4 结果一次性返回，无渐进显示

`VisionJobManager.get()` 在 job 完成前 `results` 为 `None`，因此必须等整个视频跑完
才能看到任何结果，中途只有进度百分比。

单轮提交时的量级估算（**估计值**）：60 秒 × 30 FPS 抽帧 = 1800 帧，每帧 JSON 约 600~900 B，
合计约 1.4 MB，可接受。但 10 分钟约 14 MB、30 分钟约 43 MB，需要分页或流式。
这是第 3 节要求「按单轮提交」的原因。

### 5.5 结果查找是逐帧线性扫描

`ApplyVideoJobResult()` 每帧从索引 0 开始扫描 `videoResults`（命中后 break）。
随播放推进扫描长度递增，结果数达万级时每秒比较次数可观。应改为单向游标，
仅在用户跳转时重置。单轮短视频下暂不影响。

### 5.6 无开火时刻检测

后端产出逐帧检测，但没有任何环节知道玩家在哪一帧开了枪。
「开枪瞬间准心离头部多少」这一核心指标因此无法计算。见第 7 节。

### 5.7 job 记录不过期

`VisionJobManager._jobs` 只增不减，长期运行的服务会持续累积已完成 job 的完整结果。
需要加入过期清理。

---

## 6. 模型选型

### 推荐：keremberke/yolov8m-csgo-player-detection

公开可下载，无授权门槛，Ultralytics 原生 `.pt`，且**类别命名与后端 `normalize_label` 完全吻合**：

```
['ct', 'cthead', 't', 'thead']
```

`cthead` → 团队 CT + 部位 head，`thead` → 团队 T + 部位 head。
因此后端**不需要修改任何代码**即可产出 `part=head` 的检测框，
`select_recommended_aim()` 也就能正常选出头部目标。

| 变体 | 权重体积 | mAP@0.5 |
| --- | --- | --- |
| `yolov8n-csgo-player-detection` | 6.2 MB | 0.844 |
| `yolov8s-csgo-player-detection` | ~22 MB | 0.886 |
| `yolov8m-csgo-player-detection` | 52 MB | 0.892 |

**离线形态直接选 `m`**。实时形态才需要牺牲精度换速度，本方案没有这个约束——
分析慢十倍也只是多等几十秒，而检出质量是教学价值的基础。
若 CPU 上单轮分析时间不可接受，先降 `sample_rate`，不要先降模型规格。

**两个已知风险**：
- 这是 **CSGO** 模型而非 CS2。CS2 角色是重制版但外观接近，迁移效果**需实测确认**，不能假设。
- 权重由 `ultralytics 8.0.21`（2023 年 2 月）训练，而 `requirements.txt` 锁
  `ultralytics>=8.3,<9`。新版加载旧 `.pt` 通常向后兼容，但需实测确认。

### 备选：jparedesDS 系列（CS2 原生，需授权）

`cs2-yolov10s`（16.7 MB）、`cs2_yolo12s`、`cs2-yolov10m/b`、`cs2-yolov9c`。
`VISION_NEXT_STEPS_ZH.md` 第 2 节引用的即此系列，是真正的 CS2 模型。

问题是全部为 **gated=manual**，需作者手动审核。当前状态：`cs2_yolo12s` 的访问申请
**正在等待作者审核**，`cs2-yolov10s` 尚未申请。审核周期不可控，因此不作为起步依赖——
按 keremberke 起步，授权批下后替换权重即可，接口不变。

### 数据集（用于后续自训）

`CactusBooblegum/Counter-Strike-2-rectangles-yolo`：mirage 1558 张 + anubis 500 张，
YOLO 格式，打包为两个 zip 合计 5 GB。是否含头部类别未确认。

---

## 7. 开火时刻检测

这是本形态相对实时形态**额外获得**的能力，也是最核心指标的前提。

录屏文件带音频轨，而枪声的起始点在音频上特征明确、易于可靠检测。实时形态走
`WebCamTexture` 只有画面没有声音，拿不到这个信息。

**方案**：用 ffmpeg 从录像抽出音频轨，做包络/起始点检测得到开火时间戳列表，
与逐帧检测结果按时间戳对齐，即得到每一发子弹开火瞬间的准心-头部偏差。

**备选**（可靠性依次下降）：
- 弹药数 OCR：HUD 弹药数每次开火递减。固定分辨率下裁剪定位稳定，可用数字模板匹配。
- 枪口火光：纯视觉，受武器、光照、烟雾影响大。

**注意**：`VisionJobManager._run()` 使用 cv2 读取视频，会忽略音频轨，
因此音频处理是独立于现有 job 的新增环节。

**不阻塞基础分析**：准心 placement 质量（见 8.1）逐帧即可统计，不需要开火时刻。
开火检测是精度升级项，不是前置条件。

---

## 8. 分析指标

### 8.1 不需要开火检测的指标（现有代码即可支撑）

- **准心 placement 偏差分布**：每个抽样帧，屏幕中心到最近敌人头部的偏差。
- **垂直偏倾向**：偏差的纵向分量。系统性偏低是中低分段最典型的问题，教学价值最高。
- **水平偏倾向**：偏差的横向分量，反映惯用侧偏好。
- **有效跟随时长**：敌人在画面内且偏差小于阈值的时间占比。

### 8.2 需要开火检测的指标

- **开火瞬间偏差**：每一发子弹的准心-头部角度差。这是最初设计的核心指标。
- **反应时间**：敌人首次出现在画面到首次开火的间隔。
- **过量修正**：开火前准心是否越过目标再回摆。

### 8.3 像素到角度的换算

要输出「偏差 5°」而非「偏差 80 像素」，需要录像分辨率与游戏 FOV（CS2 默认水平 90）。
换算是非线性的，不能简单用 `FOV / 宽度`：

```
f     = (width / 2) / tan(FOV_h / 2)
angle = atan(pixel_offset / f)
```

因此需要把 FOV 与分辨率作为分析配置项，不能硬编码。

---

## 9. 实施阶段

### P0：跑通离线分析链路

运行机（能打 CS2 且有 NVIDIA 显卡的那一台）按仓库根目录 `AGENTS.md` 操作，不要拆成双机。

1. `.\Backend\setup-vision.ps1`：先装 CUDA 版 torch，再装其余依赖，并下载
   `keremberke/yolov8m-csgo-player-detection` 到 `models/yolov8m-csgo.pt`。
   **不要先跑 `run.ps1`**，否则会装上 CPU 版 torch。
2. `.\Backend\run-vision.ps1` 启动。确认 `GET /health` 中 `vision.cuda_available=true`、
   `enemy_model=ready`。`FPS_VISION_MEDIA_ROOT` 默认是仓库 `media\`，可在 `.env`
   改成 OBS 输出目录（解决 5.3）。
3. 录一段靶场练习，用 `IMPORT VIDEO` 提交，验证 job 完成、结果回传。
4. 验证叠加：框与画面中敌人目视对齐，含 `part=head` 的框，推荐瞄准点与偏移连线可见，
   拖动进度条时标记跟随。

**P0 的验收目标是链路正确与坐标对齐，不是检出率。**
同时在此阶段实测第 6 节的两个风险（CSGO→CS2 迁移效果、ultralytics 版本兼容）。

### P1：分析指标与靶场真值配对

- 实现 8.1 的四项指标与 8.3 的角度换算。
- 实现第 7 节的音频开火检测，接上 8.2。
- **靶场 demo + 视频同时录制**：CS2 在创意工坊图中同样可用控制台 `record` 录 demo。
  靶场环境地图单一、无烟雾闪光、目标少、光照固定，是获取配对数据的理想场景。
  两者同时录制即可从 demo 取该 tick 目标的 3D 坐标投影到屏幕，得到**免人工标注的真值框**，
  用于量化视觉模型的角度误差。这比在职业比赛 demo 上做同样的事简单得多。

### P2：精度提升

- 用 P1 得到的配对数据量化误差，据此决定是微调还是换模型。
- jparedesDS 授权批下后换 CS2 原生模型并对比。
- 用配对数据微调，或结合 `CactusBooblegum` 数据集。

### P3：工程完善

- 渐进结果返回（解决 5.4）、游标查找（5.5）、job 过期清理（5.7）。
- 录像自动发现：监听 OBS 输出目录，练习结束后自动列出新录像。

---

## 10. 合规边界

本形态在合规性上显著优于实时方案，理由是**游戏运行期间没有任何分析程序介入**：

- 玩家看到的画面完全未被改变，没有任何叠加或提示注入游戏。
- 游戏过程中运行的只有 OBS 录屏，这是极其普遍的常规行为。
- 分析发生在练习结束之后，处理的是录像文件，与任何 VOD 复盘工具性质相同。
- 不读内存、不注入进程、不输出任何形式的输入接管或自动瞄准。

即便如此，仍将定位限定为**练习工具**（靶场、训练图、创意工坊、bot、死斗），
界面上明示适用场景。竞技对局的分析需求应走 `.dem` 复盘路线（形态 B），
不要把屏幕录像分析引入正式比赛流程。

---

## 11. 验收标准

**P0 完成**：
- 视频 job 不再因缺依赖失败，`diagnostics` 显示模型 ready。
- 单轮录像提交后 job 走到 `completed` 并回传结果。
- 结果含 `part=head` 的检测框，`recommended_aim.target_id` 指向最近屏幕中心的敌人头部。
- 叠加框与大屏画面中敌人位置目视对齐，快速转视角时无系统性偏移。
- 拖动进度条、暂停、跳转后标记正确跟随。
- 记录 CSGO 模型在 CS2 画面上的实测检出情况。

**P1 完成**：
- 输出 8.1 四项指标，偏差以角度为单位。
- 音频开火检测得到的时间戳与画面开火时刻一致。
- 完成一次靶场 demo + 视频配对，能给出视觉模型的角度误差数值。

---

## 12. 未决问题

1. CSGO 模型迁移到 CS2 的实际检出率——必须实测。
2. `ultralytics 8.3+` 能否正常加载 `8.0.21` 训练的权重。
3. `m` 模型在目标机 GPU 上单轮分析耗时（`FPS_VISION_DEVICE=cuda`，见 `AGENTS.md`）。
   CPU 耗时仅作对照，不是验收环境。
4. OBS 是否已安装，录制参数（分辨率、帧率、音频轨）如何配置。
5. `CactusBooblegum` 数据集是否含头部类别。
6. 靶场 demo 中 bot 是否带完整位置数据，以及录像帧与 demo tick 的对齐精度。
