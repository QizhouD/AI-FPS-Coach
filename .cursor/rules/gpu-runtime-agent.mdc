---
description: GPU 运行机 Agent 拉仓后的环境与 P0 约束
alwaysApply: true
---

# GPU 运行机

先读仓库根目录 `AGENTS.md`，再装任何 Python 包或启动后端。

- 最终形态是单机：Unity + OBS + FastAPI + CUDA YOLO，地址保持 `127.0.0.1:8000`。
- 第一次配环境只用 `Backend/setup-vision.ps1`，禁止先跑 `Backend/run.ps1`（会装 CPU 版 torch）。
- 日常启动用 `Backend/run-vision.ps1`。P0 是本机 CUDA 跑通单轮录像，不要做开火检测、准心训练或跨机上传。
- 产品决策以 `PRACTICE_REVIEW_DESIGN_ZH.md` 为准。不要提交 `.pt`、录像或 `.dem`。
